[
  %% Kernel configuration for tests
  {kernel, [
    {error_logger, silent}
  ]},

  %% SASL configuration to suppress supervisor reports
  {sasl, [
    {sasl_error_logger, false},
    {errlog_type,       error}
  ]}
].